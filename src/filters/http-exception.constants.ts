/** HTTP status used for missing routes and resources. */
export const HTTP_EXCEPTION_NOT_FOUND_STATUS = 404;

/** HTTP status threshold above which exceptions are treated as server failures. */
export const HTTP_EXCEPTION_SERVER_ERROR_STATUS = 500;
